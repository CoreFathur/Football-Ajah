// prompts/quick.ts

export const quickFragment = (): string => {
  return `Quick analysis: focus only on Winner (home/draw/away), BTTS (yes/no), Over/Under (2.5), and a single-line confidence estimate. Keep it short (1-3 sentences).`
}
